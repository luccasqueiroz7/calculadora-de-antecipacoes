import { CalculatedValueProvider } from "./calculatedValue";

const Providers = ({ children }) => {
  return <CalculatedValueProvider>{children}</CalculatedValueProvider>;
};

export default Providers;
